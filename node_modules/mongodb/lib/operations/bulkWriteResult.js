"use strict";

class BulkWriteResult {
  constructor(result) {
    this.result = result;
  }

  get insertedCount() {
    return this.result.insertedCount || 0;
  }
}

module.exports = BulkWriteResult;
